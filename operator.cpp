// #include <iostream>
// using namespace std;

// int main() {

    /////////////////////////////////////////
    // 1️⃣ Calculate Average Marks and Pass/Fail
    /////////////////////////////////////////
    /*
    int s1, s2, s3, s4, s5;
    cout << "Enter your marks: ";
    cin >> s1 >> s2 >> s3 >> s4 >> s5;

    float avg = (s1 + s2 + s3 + s4 + s5) / 5.0;
    cout << "Average = " << avg << endl;

    if (avg >= 50)
        cout << "Pass" << endl;
    else
        cout << "Fail" << endl;
    */

    /////////////////////////////////////////
    // 2️⃣ Previous Character of a Given Character
    /////////////////////////////////////////
    /*
    char ch;
    cout << "Enter a character: ";
    cin >> ch;

    char prevChar = ch - 1; 
    cout << "Previous character: " << prevChar << endl;
    */

    /////////////////////////////////////////
    // 3️⃣ Surface Area of a Sphere
    /////////////////////////////////////////
    /*
    float radius;
    cout << "Enter radius of sphere: ";
    cin >> radius;

    float area = 4 * 3.14 * radius * radius;
    cout << "Surface area of sphere = " << area << endl;
    */

    /////////////////////////////////////////
    // 4️⃣ Check if Number is Positive and Even
    /////////////////////////////////////////
    /*
    int num;
    cout << "Enter a number: ";
    cin >> num;

    if (num > 0 && num % 2 == 0)
        cout << "Number is even and positive." << endl;
    else
        cout << "Number is not positive or not even." << endl;
    */

    /////////////////////////////////////////
    // 5️⃣ Discount Based on Bill or Membership
    /////////////////////////////////////////
    /*
    float bill;
    char member;
    cout << "Enter total bill: ";
    cin >> bill;
    cout << "Are you a regular member? (y/n): ";
    cin >> member;

    if (bill > 5000 || member == 'y' || member == 'Y')
        cout << "Customer gets a discount." << endl;
    else
        cout << "No discount." << endl;

    return 0;
}*/

// #include <iostream>
// using namespace std;

// int main() {

    /////////////////////////////////////////
    // 1️⃣ Calculate Average Marks and Pass/Fail
    /////////////////////////////////////////
    /*
    int s1, s2, s3, s4, s5;
    cout << "Enter your marks: ";
    cin >> s1 >> s2 >> s3 >> s4 >> s5;

    float avg = (s1 + s2 + s3 + s4 + s5) / 5.0;
    cout << "Average = " << avg << endl;

    if (avg >= 50)
        cout << "Pass" << endl;
    else
        cout << "Fail" << endl;
    */

    /////////////////////////////////////////
    // 2️⃣ Previous Character of a Given Character
    /////////////////////////////////////////
    /*
    char ch;
    cout << "Enter a character: ";
    cin >> ch;

    char prevChar = ch - 1; 
    cout << "Previous character: " << prevChar << endl;
    */

    /////////////////////////////////////////
    // 3️⃣ Surface Area of a Sphere
    /////////////////////////////////////////
    /*
    float radius;
    cout << "Enter radius of sphere: ";
    cin >> radius;

    float area = 4 * 3.14 * radius * radius;
    cout << "Surface area of sphere = " << area << endl;
    */

    /////////////////////////////////////////
    // 4️⃣ Check if Number is Positive and Even
    /////////////////////////////////////////
    /*
    int num;
    cout << "Enter a number: ";
    cin >> num;

    if (num > 0 && num % 2 == 0)
        cout << "Number is even and positive." << endl;
    else
        cout << "Number is not positive or not even." << endl;
    */

    /////////////////////////////////////////
    // 5️⃣ Discount Based on Bill or Membership
    /////////////////////////////////////////
//     float bill;
//     char member;
//     cout << "Enter total bill: ";
//     cin >> bill;
//     cout << "Are you a regular member? (y/n): ";
//     cin >> member;

//     if (bill > 5000 || member == 'y' || member == 'Y')
//         cout << "Customer gets a discount." << endl;
//     else
//         cout << "No discount." << endl;

//     return 0;
// }



